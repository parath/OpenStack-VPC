---
# Copyright 2016, Stackmasters.eu

- name: Install Openstack Mitaka repo
  yum:
    name: "{{ item }}"
    state: present
  with_items:
    - centos-release-openstack-mitaka

- name: Install openstackclient
  yum:
    name: "{{ item }}"
    state: present
  with_items:
    - python-openstackclient


