# {{ansible_managed}}
{% if inventory_hostname in groups['controllers'] %}
allow {{ os_br_mgmt_subnet }}
{%endif%}
{% for host in groups['controllers'] %}
server {{ hostvars[host].ansible_br_mgmt.ipv4.address }} iburst
{% endfor %}

