# {{ ansible_managed }}
[DEFAULT]

# Nova metadata service IP and port
nova_metadata_ip = {{ internal_vip }}
nova_metadata_port = {{ nova_metadata_port }}

# Metadata proxy shared secret
metadata_proxy_shared_secret = {{ neutron_metadata_agent_secret }}
